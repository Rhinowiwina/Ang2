import { CalendarType } from './../common/types/calendar-type';
import { IDatePickerDirectiveConfig } from './date-picker-directive-config.model';
import { DatePickerDirectiveService } from './date-picker-directive.service';
import { IDpDayPickerApi } from './date-picker.api';
import { DatePickerComponent } from './date-picker.component';
import { ElementRef, OnInit, ViewContainerRef, ComponentFactoryResolver } from '@angular/core';
import { NgControl } from '@angular/forms';
import { Moment } from 'moment';
export declare class DatePickerDirective implements OnInit {
    viewContainerRef: ViewContainerRef;
    componentFactoryResolver: ComponentFactoryResolver;
    formControl: NgControl;
    service: DatePickerDirectiveService;
    private _config;
    private _attachTo;
    private _theme;
    private _type;
    private _minDate;
    private _maxDate;
    config: IDatePickerDirectiveConfig;
    attachTo: ElementRef | string;
    theme: string;
    type: CalendarType;
    minDate: Moment | string;
    maxDate: Moment | string;
    datePicker: DatePickerComponent;
    api: IDpDayPickerApi;
    constructor(viewContainerRef: ViewContainerRef, componentFactoryResolver: ComponentFactoryResolver, formControl: NgControl, service: DatePickerDirectiveService);
    ngOnInit(): void;
    createDatePicker(): DatePickerComponent;
    attachModelToDatePicker(): void;
    onClick(): void;
    onFocus(): void;
    private updateDatepickerConfig();
}
